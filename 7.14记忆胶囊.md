\[START OF CONTEXT]



\# 功能医学营养推荐系统 - 记忆胶囊



\## 1. 项目目标



项目目录：`D:\\medical-deploy-test`  

GitHub：`111qaz1/functional-medicine-ai`



这是一个功能医学营养推荐工作台，服务医生/顾问与外部甲方系统：



1\. 创建病例。

2\. 上传病例报告、MSQ 问卷。

3\. 自动提取指标、问卷内容与病例文本。

4\. 医生人工校对、补录指标和病例总结诊断。

5\. 生成营养素推荐草案。

6\. 生成总医嘱说明、最终可发布报告、PDF。

7\. 医生或甲方系统审核发布后下载 PDF。

8\. 对外提供 Bearer Token + REST API 集成。



核心边界：



\- 系统提供营养支持建议，不做疾病诊断、治疗或治愈承诺。

\- 推荐、剂量、禁忌、报告发布必须保留人工审核能力。

\- 外部 API 只能访问当前 Bearer Token 医生所属病例/草案。

\- 不改变既有 `/nutrition-recommendations` 响应结构，新增能力使用独立接口。

\- 不新增不必要数据库表；尽量复用现有病例、草案、审核决定模型。



\---



\## 2. 当前代码状态



当前分支：



```text

codex/fix-approval-and-parsing

```



最近提交：



```text

1adf73f Add external draft approval endpoint

2322354 OCR PDFs only when text layer is unreadable

5c26b61 Keep PDF OCR switch implicit

5df78ad Disable PDF image OCR by default

8410b25 Improve report editing and summary formatting

```



当前无正式语义版本号；以 Git commit/PR 为准。



已有 PR：



```text

PR #12: Add prescription advice summary and startup fixes

https://github.com/111qaz1/functional-medicine-ai/pull/12

```



PR #12/当前分支已包含：



\- 外部草案审核发布接口 `POST /api/v1/drafts/{draft\_id}/approve`。

\- PDF 解析优先读取 PDF 原始文本层；只有文本层为空或不可用时才走图像 OCR。

\- 不再使用 `FM\_PDF\_IMAGE\_OCR\_ENABLED=0` 作为长期配置开关。

\- 总医嘱说明加入报告和 PDF，位置固定为：

&#x20; `首月营养素干预方案` -> `总医嘱说明` -> `推荐搭配说明`。

\- 总医嘱说明是草案级说明，不再是逐条处方项目。

\- 推荐草案审核编辑区已扩展，支持大编辑器、格式整理逻辑等前端优化。

\- 已修复报告正文标题粘连、段落错行等格式问题。



已验证测试：



```powershell

python -m unittest backend.tests.test\_external\_api backend.tests.test\_pdf\_export backend.tests.test\_recommendation

\# 56 tests OK



python -m unittest backend.tests.test\_parsing backend.tests.test\_llm\_config

\# 33 tests OK

```



\---



\## 3. 技术栈



\### 后端



\- Python

\- FastAPI `0.115.12`

\- Uvicorn `0.34.2`

\- Pydantic `2.11.3`

\- python-dotenv `1.2.1`

\- httpx `0.28.1`

\- python-multipart `0.0.20`

\- SQLite（运行数据在 `.runtime/app.sqlite3`）

\- ReportLab `4.4.1`：PDF 生成

\- pypdf `5.4.0`：PDF 文本提取

\- python-docx `1.2.0`

\- numpy `2.4.6`

\- sentence-transformers `5.5.1`

\- FAISS CPU `1.14.2`

\- jieba `0.42.1`

\- rank-bm25 `0.2.2`

\- langchain-community `0.4.2`



\### 前端



\- Next.js `15.5.15`

\- React `19.1.0`

\- React DOM `19.1.0`

\- TypeScript

\- 前端目录：`frontend/`



\### AI / RAG



\- 默认本地向量模型：`bge-m3`

\- Docker 容器模型路径：`/models/bge-m3`

\- 宿主机目录由 `FM\_RAG\_MODEL\_HOST\_DIR` 映射，默认 `./bge-m3`

\- 本地知识目录：`knowledge/`

\- RAG 索引：`backend/app/data/rag\_index`

\- LLM 配置兼容 OpenAI 风格 API：

&#x20; - `LLM\_BASE\_URL`

&#x20; - `LLM\_API\_KEY`

&#x20; - `LLM\_MODEL`

&#x20; - `LLM\_API\_STYLE`

&#x20; - `LLM\_TIMEOUT\_SECONDS`

&#x20; - `LLM\_TEMPERATURE`



\### Docker



`compose.yaml` 默认包含两个容器：



\- `backend`：FastAPI，内部端口 `8000`

\- `frontend`：Next.js，内部端口 `3000`



默认 Compose 端口绑定为本机回环地址：



```yaml

127.0.0.1:${BACKEND\_PORT:-8000}:8000

127.0.0.1:${FRONTEND\_PORT:-3000}:3000

```



如需局域网跨机器访问，甲方可修改为：



```yaml

"${BACKEND\_PORT:-8000}:8000"

"${FRONTEND\_PORT:-3000}:3000"

```



即容器映射为 `0.0.0.0:8000`、`0.0.0.0:3000`。还需放行服务器防火墙端口。



Nginx 不在当前 Docker Compose 内，不会自动部署；若生产环境需 HTTPS/域名，应由甲方自行部署 Nginx 反代。



\---



\## 4. 重要目录与运行数据



```text

backend/                     FastAPI 后端

backend/app/api/             内部与外部 API

backend/app/services/        核心业务服务

backend/tests/               单元测试

frontend/                    Next.js 前端

knowledge/                   知识库，只读挂载到容器

bge-m3/                      本地 RAG 模型目录，通常不进 Git

.runtime/                    运行时数据目录，不进 Git

.runtime/app.sqlite3         SQLite 数据库

.runtime/uploads/            上传文件

compose.yaml                 Docker Compose 编排

.env                         实际部署配置，不进 Git

.env.example                 配置样例

```



`.runtime` 位于项目根目录，即：



```bash

/opt/functional-medicine-ai-main/.runtime

```



升级部署时通常必须保留：



```text

.env

.runtime/

bge-m3/

knowledge/（如甲方维护了自定义知识）

```



\---



\## 5. 认证与外部 API 协议



外部 API 前缀：



```text

/api/v1

```



外部接口认证方式：



```http

Authorization: Bearer <access\_token>

```



Token 获取接口：



```http

POST /api/v1/auth/token

```



请求字段：



```json

{

&#x20; "issuer": "customer-system",

&#x20; "doctor\_id": "doctor-001",

&#x20; "doctor\_name": "外部医生A",

&#x20; "timestamp": 1760000000,

&#x20; "nonce": "random-nonce-at-least-8-chars",

&#x20; "signature": "hmac\_sha256\_hex"

}

```



签名原文必须严格按以下顺序、以换行符拼接：



```text

issuer

doctor\_id

doctor\_name

timestamp

nonce

```



计算方式：



```text

HMAC-SHA256(FM\_EXTERNAL\_TRUST\_SHARED\_SECRET, canonical\_payload).hexdigest()

```



规则：



\- `signature` 可以是纯 hex，或 `sha256=<hex>`。

\- `timestamp` 与服务器时间偏差超过 300 秒会被拒绝。

\- `nonce` 最少 8 位。

\- `signature` 最少 32 位。

\- Token 有效期为 14 天。

\- `FM\_EXTERNAL\_TRUST\_SHARED\_SECRET` 是双方共享密钥，必须高强度、不得放前端、不得提交 Git。

\- 之前截图/聊天中曾暴露真实 LLM Key 和共享密钥，部署方应立即轮换这些密钥。



\---



\## 6. 当前已实现的外部 API



\### 6.1 创建病例



```http

POST /api/v1/cases

```



请求：



```json

{

&#x20; "customer\_name": "张三",

&#x20; "consultant\_id": "医生或顾问标识",

&#x20; "notes": "可选",

&#x20; "analysis\_mode": "llm\_primary"

}

```



`analysis\_mode` 主要值：



```text

llm\_primary

local\_knowledge\_first

```



返回包含：



```json

{

&#x20; "case\_id": "case\_xxx",

&#x20; "status": "created",

&#x20; "customer\_name": "张三",

&#x20; "owner\_doctor\_id": "doctor\_xxx"

}

```



\### 6.2 上传病例或问卷



```http

POST /api/v1/cases/{case\_id}/attachments

Content-Type: multipart/form-data

```



字段：



```text

files: 一个或多个文件

attachment\_type: case | questionnaire

```



规则：



\- `attachment\_type=case`：病例/化验报告解析，提取文本、指标、置信度、警告。

\- `attachment\_type=questionnaire`：按 MSQ 问卷逻辑导入。

\- 两者共用一个上传路径，但通过 `attachment\_type` 分流，已经达到“一个上传接口、分别识别病例或问卷”的要求。

\- 病例可上传多个文件。

\- 问卷不是生成草案的强制前置条件；没有问卷也可生成草案，但报告会提示信息缺失。

\- 上传接口当前是同步处理：HTTP 返回成功即表示该文件已解析完成，不存在独立异步“解析中”轮询状态接口。



返回示意：



```json

{

&#x20; "case\_id": "case\_xxx",

&#x20; "status": "parsing\_completed",

&#x20; "results": \[

&#x20;   {

&#x20;     "file\_id": "file\_xxx",

&#x20;     "filename": "report.pdf",

&#x20;     "attachment\_type": "case",

&#x20;     "status": "parsed",

&#x20;     "lab\_item\_count": 12,

&#x20;     "parse\_warnings": \[]

&#x20;   }

&#x20; ]

}

```



\### 6.3 生成营养素推荐草案



```http

POST /api/v1/cases/{case\_id}/nutrition-recommendations

```



返回：



```json

{

&#x20; "case\_id": "case\_xxx",

&#x20; "draft\_id": "draft\_xxx",

&#x20; "status": "pending\_review",

&#x20; "manual\_review\_required": true,

&#x20; "confidence": 0.89,

&#x20; "recommendations": \[

&#x20;   {

&#x20;     "sku\_id": "sku\_xxx",

&#x20;     "name": "产品名称",

&#x20;     "category": "营养素",

&#x20;     "dosage": "推荐剂量",

&#x20;     "reason": "推荐依据",

&#x20;     "warnings": \[]

&#x20;   }

&#x20; ],

&#x20; "contraindications": \[],

&#x20; "missing\_info": \[]

}

```



必须理解：



\- `pending\_review` 与 `manual\_review\_required=true` 是正常业务状态，不是失败。

\- 该接口只生成草案，不代表正式发布。

\- 可通过以下接口获取最新草案：



```http

GET /api/v1/cases/{case\_id}/nutrition-recommendations/latest

```



\### 6.4 总医嘱说明



沿用历史路径，当前语义已改为草案级“总医嘱说明”：



```http

GET /api/v1/drafts/{draft\_id}/prescription-items

```



返回：



```json

{

&#x20; "case\_id": "case\_xxx",

&#x20; "draft\_id": "draft\_xxx",

&#x20; "status": "pending\_review",

&#x20; "medical\_advice": "处方级营养素用于补充身体当下所需营养，予以肝脏解毒代谢支持、免疫调节等方向的营养支持，帮助平衡免疫、抗炎、抗氧化及代谢调节。",

&#x20; "advice\_source": "llm"

}

```



约束：



\- 不再返回逐条 `items`、`course\_period`、逐条 `dosage`、逐条 `medical\_advice`。

\- `medical\_advice` 目标 50-100 个中文字符。

\- LLM 可用时润色；失败、超时、未配置、包含内部话术时走 `local\_fallback`。

\- 禁止治疗承诺、`对症治疗`、RAG、关联度、命中产品标签、路径、密钥等内部内容。

\- 报告/PDF 固定插入“总医嘱说明”。



\### 6.5 外部审核发布接口



已实现：



```http

POST /api/v1/drafts/{draft\_id}/approve

```



请求：



```json

{

&#x20; "reviewer\_id": "doctor-001",

&#x20; "publishable\_summary": "甲方/医生确认后的最终报告 Markdown，可选",

&#x20; "edits": {}

}

```



返回：



```json

{

&#x20; "case\_id": "case\_xxx",

&#x20; "draft\_id": "draft\_xxx",

&#x20; "status": "approved",

&#x20; "report\_ready": true,

&#x20; "filename": "xxx.pdf",

&#x20; "download\_url": "http://host:8000/api/v1/drafts/draft\_xxx/report.pdf"

}

```



这已经满足甲方“系统拿到 JSON 草案后，由甲方 POST 回确认草案，正式确认发布”的核心要求。



审核后获取下载地址：



```http

GET /api/v1/drafts/{draft\_id}/report-download

```



下载 PDF：



```http

GET /api/v1/drafts/{draft\_id}/report.pdf

```



未审核时下载接口返回：



```text

409 Report is not approved yet

```



\---



\## 7. 内部工作台接口：已存在但尚未对外暴露



以下能力目前仅供工作台 Cookie Session 使用，外部 `/api/v1` 尚未实现同等接口：



```http

PUT /cases/{case\_id}/clinical-summary

POST /cases/{case\_id}/clinical-summary-image

PUT /cases/{case\_id}/parsing-review

GET /drafts/{draft\_id}

POST /drafts/{draft\_id}/approve

GET /drafts/{draft\_id}/report.pdf

```



其中：



\### 7.1 病例总结诊断



工作台的“保存病例总结诊断”调用：



```http

PUT /cases/{case\_id}/clinical-summary

```



保存字段：



```text

clinical\_summary\_text

```



这段内容会参与：



\- 系统支持方向分析。

\- 营养素候选排序。

\- 推荐草案理由。

\- 最终报告生成。



\### 7.2 保存解析校对



工作台“保存解析校对”调用：



```http

PUT /cases/{case\_id}/parsing-review

```



它可以：



\- 确认/修正解析出的化验指标 `normalized\_lab\_items`。

\- 补录手动指标 `manual\_indicators`。

\- 修正上传文本、缺失字段和校对备注。

\- 标记 `parsing\_review\_completed=true`。

\- 消除“尚未完成人工解析校对”的风险提示。



术语区分：



\- `normalized\_lab\_items`：结构化化验指标，如 ALT、LDL、TSH、数值、单位、参考范围。

\- `manual\_indicators`：人工补录的非标准化发现，如脂肪肝、甲状腺结节、骨量减少、影像结论、体检总结等。



手动指标建议结构：



```json

{

&#x20; "indicator\_name": "脂肪肝",

&#x20; "result\_text": "总检提示阳性/异常",

&#x20; "status": "attention",

&#x20; "evidence\_text": "体检总结提示脂肪肝"

}

```



\---



\## 8. 当前外部交付流程的缺口与建议



外部系统完整流程目前还不够顺畅，原因不是生成草案本身，而是工作台中间存在“解析校对、病例总结诊断、最终报告草案”的交互步骤，但对外 API 尚未全部开放。



建议按优先级新增以下接口。



\### P0：外部解析校对接口



建议新增：



```http

GET /api/v1/cases/{case\_id}/parsing-review

POST /api/v1/cases/{case\_id}/parsing-review

```



用途：



1\. GET 返回解析后的病例文本、文件状态、已提取指标、解析警告、待补充项。

2\. 甲方展示给医生。

3\. 医生确认、修正、补录。

4\. POST 保存后再调用推荐草案接口。



建议 POST 请求结构：



```json

{

&#x20; "reviewer\_id": "doctor-001",

&#x20; "files": \[

&#x20;   {

&#x20;     "file\_id": "file\_xxx",

&#x20;     "corrected\_text": "可选，修正后的文本",

&#x20;     "missing\_fields": \[]

&#x20;   }

&#x20; ],

&#x20; "normalized\_lab\_items": \[],

&#x20; "manual\_indicators": \[],

&#x20; "missing\_fields": \[],

&#x20; "review\_notes": "可选"

}

```



这样医生可直接补录/修正指标，数据会进入后续推荐与报告。



\### P0：外部病例总结诊断接口



建议新增：



```http

GET /api/v1/cases/{case\_id}/clinical-summary

PUT /api/v1/cases/{case\_id}/clinical-summary

```



PUT 示例：



```json

{

&#x20; "clinical\_summary\_text": "脂肪酸代谢不佳、碳水化合物代谢不佳、细胞能量生成反应不佳。"

}

```



理由：这是工作台实际存在且影响推荐与最终报告的关键输入。若甲方希望完整复刻工作台流程，应提供此接口。



\### P1：外部最终报告草案接口



当前外部接口只返回营养素草案 JSON，没有直接返回工作台中“审核后发布内容”的完整 Markdown。



建议新增：



```http

GET /api/v1/drafts/{draft\_id}/publishable-report

```



建议返回：



```json

{

&#x20; "case\_id": "case\_xxx",

&#x20; "draft\_id": "draft\_xxx",

&#x20; "status": "pending\_review",

&#x20; "format": "markdown",

&#x20; "publishable\_report": "# 功能医学综合分析与首月干预方案\\n...",

&#x20; "report\_ready": false

}

```



生成逻辑必须复用 `review\_local.py` 的最终报告渲染逻辑，避免外部看到的文本与 PDF 不一致。



推荐的完整外部流程：



```text

获取 Token

\-> 创建病例

\-> 上传病例/问卷

\-> GET 解析校对数据

\-> 医生补录/修正并 POST 保存校对

\-> PUT 病例总结诊断（可选但推荐）

\-> POST 生成营养素草案

\-> GET 总医嘱说明

\-> GET 最终报告草案

\-> 甲方展示/编辑/确认

\-> POST approve

\-> GET report.pdf 下载最终 PDF

```



不要删除“解析校对”这个业务步骤；应将其外部化。完全跳过校对会降低病例指标质量，并与现有人工审核设计相冲突。可未来增加 `auto\_confirm\_parsing=true` 作为明确的自动确认选项，但默认不建议开启。



\---



\## 9. 报告与推荐架构原则



1\. 推荐草案与正式报告是不同状态。

&#x20;  - 草案：`pending\_review`

&#x20;  - 审核后：`approved`

&#x20;  - 未批准不能下载正式 PDF。



2\. `publishable\_summary` 是最终发布内容来源。

&#x20;  - 甲方可在外部审核接口中传入。

&#x20;  - 未传时，后端按草案和病例内容生成。

&#x20;  - PDF 必须从审核后的最终内容生成。



3\. 不允许 LLM 自由更改剂量、SKU、禁忌或做治疗承诺。

&#x20;  - LLM 仅用于报告/总医嘱的受约束文案润色。

&#x20;  - LLM 失败必须本地兜底，接口仍返回正常结果。



4\. 总医嘱说明：

&#x20;  - 草案级而非单品级。

&#x20;  - 基于推荐理由、异常指标、系统失衡分析。

&#x20;  - 使用“营养支持、营养干预、对症支持”，不用“治疗、对症治疗、治愈、疗效”等表述。



5\. “体重/BMI 异常”不能无依据推导为甲状腺异常。

&#x20;  - 系统分析必须基于已有指标、症状、病例总结或问卷。

&#x20;  - 甲状腺内容只能在实际存在甲状腺指标、抗体、病史、症状证据时出现。



6\. 报告编辑格式：

&#x20;  - Markdown 标题前后应保留空行。

&#x20;  - 防止 `###1.`、`。 ###2.` 等标题粘连。

&#x20;  - 报告导出前需统一格式化，避免标题跑到上一段末尾。



\---



\## 10. PDF / 图片解析性能问题与修复



曾出现甲方服务器上传 10MB PDF 后耗时 3-4 分钟、页面像卡死的问题。



根因：



\- 原先 PDF 可能逐页转图片再调远程 OCR/LLM。

\- 甲方服务器到模型服务网络慢。

\- 大模型优先模式、远程 OCR、服务器 CPU/内存、磁盘 IO 都会放大延迟。

\- 前端上传后没有足够明显的处理中提示时，用户会误认为卡死。



已修复策略：



```text

PDF 优先用 pypdf 提取文本层

\-> 文本可用：本地解析，不调用图像 OCR

\-> 文本层为空/不可读：才降级为图像 OCR

```



因此：



\- 正常的可复制文字 PDF 应明显比以前快。

\- 扫描件 PDF、图片型 PDF 仍可能较慢，因为必须 OCR。

\- PNG/JPG 需要图像理解能力；纯文本模型无法可靠识别图片。

\- 使用 Qwen Omni/视觉模型可以改善图片 OCR，但模型名称、API 兼容模式必须由实际厂商文档验证。



甲方 `.env` 曾出现严重配置问题：



```env

LLM\_BASE\_URL=https://...

LLM\_API\_KEY=...

LLM\_MODEL=qwen...

LLM\_API\_STYLE=chat



LLM\_BASE\_URL=

LLM\_API\_KEY=

LLM\_MODEL=

LLM\_API\_STYLE=auto

```



后面空值会覆盖前面有效值，导致 LLM 实际未配置。必须只保留一组 LLM 配置。



\---



\## 11. 部署与 `.env` 原则



执行 Docker 命令的目录必须是包含 `compose.yaml` 的项目根目录，例如：



```bash

cd /opt/functional-medicine-ai-main

docker compose up -d --build

```



日常启动：



```bash

docker compose up -d

```



查看：



```bash

docker compose ps

docker compose logs -f backend

```



`.env` 规则：



\- 以项目根目录实际 `.env` 为准。

\- `.env.example` 只是示例。

\- 部署说明文档应与当前 `.env.example` 保持同步。

\- `.env`、`.runtime`、模型目录不应被 Git 覆盖。

\- 更新代码前备份：



```bash

cp .env .env.bak

```



如果甲方无法 `git pull`，可用新代码压缩包覆盖代码文件，但必须保留：



```text

.env

.runtime/

bge-m3/

knowledge/（如有自定义内容）

```



随后重新构建：



```bash

cd /opt/functional-medicine-ai-main

docker compose down

docker compose up -d --build

```



若只更新后端：



```bash

docker compose up -d --build backend

```



但本次涉及前端或 `compose.yaml` 时，应完整执行：



```bash

docker compose up -d --build

```



\---



\## 12. CORS 与跨网访问



`NEXT\_PUBLIC\_API\_BASE\_URL` 是浏览器前端请求后端 API 的地址。



示例：



```env

NEXT\_PUBLIC\_API\_BASE\_URL=http://172.16.0.140:8000

```



CORS 的作用：浏览器默认不允许网页跨域请求其他地址/端口；当前端运行在 `http://服务器IP:3000`、后端在 `http://服务器IP:8000` 时，属于跨域，需要后端允许该来源。



生产环境建议：



```env

FM\_CORS\_ALLOW\_ORIGINS=https://实际域名

FM\_SESSION\_COOKIE\_SECURE=1

NEXT\_PUBLIC\_API\_BASE\_URL=https://实际域名

```



仅内网 HTTP 测试时：



```env

FM\_SESSION\_COOKIE\_SECURE=0

```



跨机器访问失败时，按顺序检查：



1\. `docker compose ps` 是否显示 `0.0.0.0:3000->3000/tcp`、`0.0.0.0:8000->8000/tcp`。

2\. Ubuntu 防火墙/云安全组是否放行 3000、8000。

3\. `NEXT\_PUBLIC\_API\_BASE\_URL` 是否是可从浏览器访问的服务器 IP/域名，不能写成浏览器自身的 `localhost`。

4\. 修改前端环境变量后必须重新构建 frontend：



```bash

docker compose up -d --build frontend

```



5\. 浏览器强制刷新或清理缓存。



\---



\## 13. 已发现的前端/部署现象



\### `Failed to fetch`



典型原因不是后端挂掉，而是前端构建时注入了错误的 `NEXT\_PUBLIC\_API\_BASE\_URL`，例如 `localhost:8000`。用户从另一台机器访问 `服务器IP:3000` 时，浏览器会请求“用户自己电脑的 localhost:8000”，因此失败。



\### Chrome 控制台的提示



```text

Unchecked runtime.lastError: The message port closed...

Content unavailable. Resource was not cached

```



多为浏览器扩展、DevTools 缓存或资源面板提示，不是后端业务卡死的直接证据。应优先查看：



\- Network 面板中卡住的 Fetch/XHR。

\- 后端 `docker compose logs -f backend`。

\- 浏览器 Console 的实际 API 请求错误。



\### 后端日志的健康检查很多



```text

GET /health 200 OK

```



这是 Docker healthcheck，正常，不代表业务请求卡死。



\---



\## 14. 安全权限现状与待修复项



当前已知风险：



\- 首页登录前可见 SKU、AI、模型入口卡片。

\- 部分系统配置读取接口和助手规则管理权限不足严格。

\- 应做到：仅管理员可访问 SKU、AI、模型配置、助手规则管理。



建议后续修复：



1\. 前端未登录/非管理员隐藏管理入口。

2\. 后端所有管理 API 统一 `\_require\_admin`。

3\. LLM 配置读取接口不得返回完整 API Key。

4\. 产品规则、助手规则的创建/修改/删除仅管理员可执行。

5\. 医生仅能管理自己的病例和草案。



\---



\## 15. 推荐下一步实施顺序



1\. 新增对外 `GET/POST /api/v1/cases/{case\_id}/parsing-review`。

2\. 新增对外 `GET/PUT /api/v1/cases/{case\_id}/clinical-summary`。

3\. 新增对外 `GET /api/v1/drafts/{draft\_id}/publishable-report`。

4\. 补齐对应 `backend/tests/test\_external\_api.py`。

5\. 加强后台管理权限和前端入口控制。

6\. 为外部接口提供 Postman Collection/完整示例。

7\. 甲方生产环境配置域名 + Nginx + HTTPS 后，再统一处理 CORS 与安全 Cookie。



最终建议测试：



```powershell

python -m unittest backend.tests.test\_external\_api backend.tests.test\_pdf\_export backend.tests.test\_recommendation

python -m unittest backend.tests.test\_parsing backend.tests.test\_llm\_config

```



\---



\## 16. 不要重复犯的错误



\- 不要把 `.env.example` 当实际生效配置。

\- 不要在 `.env` 中重复定义 `LLM\_\*`，后面的空值会覆盖前面的真实配置。

\- 不要提交/截图/聊天中暴露 API Key、共享签名密钥。

\- 不要把外网/局域网访问端口仍绑定 `127.0.0.1`。

\- 不要修改 `NEXT\_PUBLIC\_API\_BASE\_URL` 后只重启 backend；必须重新构建 frontend。

\- 不要把 PDF 默认全部送 OCR；先抽取文本层。

\- 不要移除解析校对，应对外提供校对接口。

\- 不要让外部系统只能生成草案、却无法提交正式审核。

\- 不要让 LLM 改 SKU、剂量、禁忌或生成治疗承诺。

\- 不要让 BMI/体重异常无证据关联甲状腺异常。

\- 不要修改现有 `/api/v1/cases/{case\_id}/nutrition-recommendations` 结构以塞入额外报告字段；新增独立接口。

\- 不要用内部工作台 Cookie 审核接口替代外部 Bearer Token 接口。



\[END OF CONTEXT]

